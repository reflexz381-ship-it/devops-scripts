name: Test Scripts

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create test file
        run: echo "this is an error log" > test.log
      - name: Run log scanner
        run: bash upgrade.sh test.log
